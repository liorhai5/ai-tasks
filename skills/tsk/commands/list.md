Call ai-tasks-list with any filters from $ARGUMENTS (e.g. `--project P`, `--status S`).

Present the results as a readable table showing project, ID, status, priority, and title.
Include the status summary (counts by status) at the top.
If no tasks found, say so and suggest creating one.
